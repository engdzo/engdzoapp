<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jamyangthinley.engdzoterminologyapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo93"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="ENG-DZO Terminology"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Computer"
            android:label="གློག་རིག/Computer" />
        <activity android:name=".Finance" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/AppTheme2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <activity android:name=".DefinitionActivity"></activity>
    </application>

</manifest>